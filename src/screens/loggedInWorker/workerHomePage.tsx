import React from 'react';
import {WorkerMain} from '../../components/loggedInWorker/workerHome';

export default function WorkerHomePage(){
    return(
        <div>
            <WorkerMain/>
        </div>
    );
}